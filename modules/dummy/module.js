
import config from './config';

const newPuzzle = (puzzleCompletedCallback) => {

};

const getConfig = () => {
  return config;
};

const getState = () => {

};

export { newPuzzle, getconfig, getState };
